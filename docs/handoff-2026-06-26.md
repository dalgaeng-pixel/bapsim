# 2026-06-26 작업 인수인계

## 현재 상태

- 이어받기 기준 문서: `docs/developer-notes.md`
- 앱 이름: 밥심 식사배달관리
- 프로젝트 위치: `C:\Users\이나라솔-PC\Desktop\앱개발\bapsim-meal-manager`
- 현재 실행 URL: `http://127.0.0.1:3000`
- HTTPS 배포 URL: `https://bapsim-meal-delivery.workspace-398477.chatgpt-team.site`
- 저장 방식: `.env.local`이 없으면 localStorage, Supabase 값이 있으면 `/api/state`를 통해 Supabase 저장

## 테스트 계정

- 관리자: 전화번호 입력 후 인증번호 4자리 이상 아무 값 입력
- 거래처 담당자: PIN `1234`

## 현재 구현 완료

- PWA 기본 설정, 앱 아이콘, 밥심 로고 적용
- 관리자 화면
  - 오늘 현황
  - 중요 변경
  - 거래처 관리
  - 배달표
  - 월별 집계
  - 설정
- 거래처 관리
  - 등록
  - 수정
  - 일시중지/사용 재개
  - PIN 재발급
- 고객 화면
  - PIN 인증
  - 오늘 식수 확인
  - 수량 변경
  - 식사 거절
  - 변경 내역
  - 업체 정보 변경 요청
  - 알림 목록
- CSV 다운로드
- 주소 클릭 시 네이버지도 검색 연결
- Supabase 스키마 및 상태 매핑 API 준비

## 검증 완료

- `npm run typecheck`
- `npm run build`
- `npm audit --omit=dev`
- `/`, `/admin`, `/client`, `/api/state` 응답 확인

## 내일 할 일

1. 휴대폰에서 HTTPS 배포 URL 접속 및 홈 화면 추가 테스트
2. 거래처 테스트용으로 필요하면 Sites 접근 권한 공개 전환
3. Supabase 프로젝트 생성
4. `.env.local` 작성
5. `docs/supabase-schema.sql` 실행
6. `/api/state`가 `mode: "supabase"`로 응답하는지 확인
7. localStorage 전체 상태 동기화 방식을 기능별 서버 액션으로 분리
8. 관리자 전화번호 OTP 실제 연결
9. 고객 초대 링크/QR 화면 고도화
10. 푸시 알림 실제 구독/발송 연결

## 다시 실행하는 방법

```bash
cd C:\Users\이나라솔-PC\Desktop\앱개발\bapsim-meal-manager
npm run dev -- --hostname 127.0.0.1 --port 3000
```
